import React from 'react'
import styled from 'styled-components';

const Container = styled.div`
  border: 1px solid #ccc;
  font-size: 1.5rem;
`;

const Header = () => {
  return (
    <Container>xChange</Container>
  )
}

export default Header